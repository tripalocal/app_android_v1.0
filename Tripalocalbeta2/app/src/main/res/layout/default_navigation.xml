<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffe5e5e5">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/default_nav_logo"
        android:textSize="21sp"
        android:gravity="center"
        android:layout_marginTop="30dp"
        android:text="Tripalocal"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:layout_marginTop="30dp"
        android:gravity="center_horizontal"
        android:layout_below="@id/default_nav_logo"
        android:id="@+id/default_nav_info"
        android:fontFamily="light"
        android:text="Please log in to access your profile"/>


    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/blue"
        android:id="@+id/nav_normal_login"
        android:layout_below="@id/default_nav_info"
        android:layout_margin="40dp"
        android:textAlignment="center"
        android:text = "Log In"
        android:textColor="@color/white"
        />

    <ImageView android:id="@+id/testBtn"
        android:layout_height="40dp"
        android:layout_width="40dp"
        android:layout_below="@id/nav_normal_login"
        android:scaleType="fitCenter"
        android:src="@drawable/heart"
        android:layout_marginLeft="12dp" />



</RelativeLayout>